<%- include('./includes/head.ejs') %>

<!--Main Start -->
<main class="main">
  <div class="heading">
    <h2 class="heading_msg">Register</h2>
  </div>
  <div class="form_div">
    <form action="/user/register">
      <div class="form_element">
        <label for="name">Name</label>
        <input
          type="text"
          required="true"
          autocomplete="off"
          id="name"
          name="name"
        />
      </div>
      <div class="form_element">
          <label for="email">Email</label>
          <input
            type="email"
            required="true"
            autocomplete="off"
            id="email"
            name="email"
          />
        </div>
        <div class="form_element">
          <label for="password">Password</label>
          <input
            type="password"
            required="true"
            autocomplete="off"
            id="password"
            name="password"
          />
        </div>
      <div class="form_element">
        <label for="confirm">Confirm Password</label>
        <input
          type="password"
          required="true"
          autocomplete="off"
          id="confirm"
          name="confirm"
        />
      </div>
      <div class="submitbtn">
      <button class="button" type="submit">Submit</button>
    </div>
    </form>
  </div>
</main>

<!-- Main Ends -->

<%- include('./includes/end.ejs') %>